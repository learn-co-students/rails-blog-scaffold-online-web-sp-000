<%= form_with(model: a_post, local: true) do |form| %>
  <% if a_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(a_post.errors.count, "error") %> prohibited this a_post from being saved:</h2>

      <ul>
      <% a_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
